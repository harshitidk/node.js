<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Welcome to My Node.js App</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <style>
    body {
      background: linear-gradient(135deg, #00B7FF 0%, #6a82fb 100%);
      color: #fff;
      height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      font-family: 'Segoe UI', 'Lucida Grande', Helvetica, Arial, sans-serif;
      margin: 0;
      overflow: hidden;
    }
    .intro-container {
      background: rgba(0,0,0,0.3);
      padding: 40px 60px;
      border-radius: 20px;
      box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
      text-align: center;
      max-width: 500px;
      display: flex;
      flex-direction: column;
      align-items: center;
    }
    .profile-img {
      width: 120px;
      height: 120px;
      object-fit: cover;
      border-radius: 50%;
      border: 4px solid #fff;
      box-shadow: 0 2px 12px rgba(0,0,0,0.15);
      margin-bottom: 1.2em;
      background: #eee;
    }
    .name {
      font-size: 2rem;
      font-weight: bold;
      margin-bottom: 0.2em;
      letter-spacing: 1px;
    }
    .title {
      font-size: 1.2rem;
      color: #b3e5fc;
      font-weight: 500;
    }
    h1 {
      font-size: 2.8rem;
      margin-bottom: 0.5em;
      letter-spacing: 2px;
    }
    p {
      font-size: 1.3rem;
      margin-bottom: 2em;
      color: #e0e0e0;
    }
    .cta-btn {
      background: #fff;
      color: #00B7FF;
      border: none;
      padding: 15px 40px;
      border-radius: 30px;
      font-size: 1.1rem;
      font-weight: bold;
      cursor: pointer;
      transition: background 0.2s, color 0.2s;
      box-shadow: 0 4px 14px rgba(0,0,0,0.15);
      text-decoration: none;
      display: inline-block;
    }
    .cta-btn:hover {
      background: #00B7FF;
      color: #fff;
    }
    @media (max-width: 600px) {
      .intro-container {
        padding: 20px 10px;
      }
      h1 {
        font-size: 2rem;
      }
    }
  </style>
</head>
<body>
  <div class="intro-container">
    <img src="/images/pfp.jpeg" alt="Harshit Profile" class="profile-img">
    <div class="name">Harshit</div>
    <div class="title">Product Designer</div>
    <p>Kickstart your journey with Express and EJS.<br>Build fast, scalable, and beautiful web applications.</p>
    <a href="https://expressjs.com/" class="cta-btn" target="_blank">Learn More</a>
  </div>
</body>
</html>
